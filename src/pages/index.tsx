import React from 'react';
import type { NextPage } from 'next';

import Calendar from '../ui-component/calendar';

const Home: NextPage = () => (
  <div>
    <Calendar />
  </div>
);

export default Home;
